
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: overcloudnets.osp-director.openstack.org
spec:
  group: osp-director.openstack.org
  names:
    kind: OvercloudNet
    listKind: OvercloudNetList
    plural: overcloudnets
    singular: overcloudnet
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: OvercloudNet is the Schema for the overcloudnets API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: OvercloudNetSpec defines the desired state of OvercloudNet
          properties:
            networks:
              description: Networks a list of networks
              items:
                description: NetworkConfig contains a Name, CIDR, and reservedIPs
                properties:
                  CIDR:
                    description: CIDR the CIDR to use for this network
                    pattern: ^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])(\/(3[0-2]|[1-2][0-9]|[0-9]))$
                    type: string
                  name:
                    description: Name the name of this Network
                    type: string
                  reservedIPs:
                    description: ReservedIPs a set of IPs that are reserved and will
                      not be assigned
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                required:
                - CIDR
                - name
                type: object
              type: array
              x-kubernetes-list-map-keys:
              - CIDR
              x-kubernetes-list-type: map
          type: object
        status:
          description: OvercloudNetStatus defines the observed state of OvercloudNet
          type: object
      type: object
  version: v1beta1
  versions:
  - name: v1beta1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
