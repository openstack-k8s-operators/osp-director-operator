#
# First, create all OpenStackNetConfig with single ipv6 net
#

apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: oc apply -f ../../../../config/samples/osp-director_v1beta1_openstacknetconfig_single_net_ipv6.yaml
    namespaced: true
